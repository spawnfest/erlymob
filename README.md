Erlymob
=======

emob
----

*Build*:

```text
cd emob
make deps
make compile
make console
```

*Set Up*:

```text
emob:setup().
```

*Deploy":

```text
emob:start().
```

emob_ui
------

*Build*:

```text
cd emob_ui
make deps
make compile
```

*Deploy*:

```text
./init-dev.sh
```
